name: run-shadow-4asset

on:
  schedule:
    - cron: '30 21 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: shadow-4asset
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - run: pip install -r requirements.txt

      - run: python run_shadow_4asset.py

      - name: Commit updated shadow log
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add shadow_4asset_log.csv
          git diff --staged --quiet || git commit -m "Update shadow 4-asset log $(date +'%Y-%m-%d')"
          git push
